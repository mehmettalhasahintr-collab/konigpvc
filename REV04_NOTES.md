# KÖNİG DIGITAL PLATFORM — REV04

Bu sürüm REV03 kod tabanı korunarak yalnızca üç alanı revize eder:

1. `/renkler` — premium architectural window color/decor studio. 9 catalog color names are retained; selecting a color changes only the simulated PVC frame/decor layer. The scene, glazing, hardware and lighting remain fixed. The visual is explicitly labeled as a visual product experience, not a real KÖNİG product photograph.
2. `/sistemler/:id` — existing real profile images are unchanged. Hotspots are positioned over visible technical regions and open a detailed panel using the product data already stored in `data/db.json`. Zoom/pan remains available.
3. `/konig` — expanded editorial/architectural brand presentation using the existing real factory images and existing verified platform data.

No project references were added back to the public site. Header, footer, admin, quote backend, comparison, technical center, production, catalog and contact structures were left intact.
